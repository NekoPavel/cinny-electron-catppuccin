{
  "name": "cinny-electron",
  "private": true,
  "version": "1.1.1",
  "description": "Electron wrapper around Cinny, Yet another matrix client",
  "main": "./out/main/index.js",
  "author": "Enova",
  "type": "module",
  "homepage": "https://github.com/Enovale/cinny-electron",
  "packageManager": "pnpm@10.20.0",
  "scripts": {
    "format": "prettier --write .",
    "lint": "eslint --cache .",
    "typecheck": "tsc --noEmit -p tsconfig.json --composite false",
    "start": "electron-vite preview",
    "dev": "electron-vite dev",
    "build": "pnpm run typecheck && electron-vite build",
    "postinstall": "electron-builder install-app-deps",
    "build:unpack": "pnpm run build && electron-builder --dir",
    "build:win": "pnpm run build && electron-builder --win",
    "build:mac": "pnpm run build && electron-builder --mac",
    "build:linux": "cd cinny && npm run build && cd .. && npm run typecheck && electron-vite build && electron-builder --linux"
  },
  "dependencies": {
    "@cinny-electron/core": "workspace:*",
    "@cinny-electron/types": "workspace:*",
    "@electron-toolkit/preload": "^3.0.2",
    "@electron-toolkit/utils": "^4.0.0",
    "@folder/xdg": "^4.0.1",
    "electron-context-menu": "^4.1.1",
    "electron-store": "^11.0.2",
    "electron-updater": "^6.6.2",
    "which": "^5.0.0"
  },
  "devDependencies": {
    "@electron-toolkit/eslint-config-prettier": "^3.0.0",
    "@electron-toolkit/eslint-config-ts": "^3.1.0",
    "@electron-toolkit/tsconfig": "^2.0.0",
    "@types/which": "^3.0.4",
    "electron": "^38.5.0",
    "electron-builder": "^25.1.8",
    "electron-vite": "^4.0.1",
    "electron-log": "^5.4.3",
    "eslint": "catalog:dev",
    "prettier": "catalog:dev",
    "typescript": "catalog:dev",
    "vite": "^7.1.12",
    "vite-tsconfig-paths": "6.0.0-beta.4"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron",
      "esbuild"
    ]
  }
}
